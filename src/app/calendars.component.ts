import { Component } from '@angular/core';
import { Router } from '@angular/router';

@Component({
  selector: 'calendars',
  moduleId: module.id,
  styleUrls: ['./all.component.css'],
  templateUrl: './calendars.component.html',

})

export class CalendarsComponent  { /*name = 'PostIt';*/
    
}
