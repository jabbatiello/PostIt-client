import { Component } from '@angular/core';
import { Router } from '@angular/router';

@Component({
  selector: 'expenses',
  moduleId: module.id,
  styleUrls: ['./all.component.css'],
  templateUrl: './expenses.component.html',

})

export class ExpensesComponent  { /*name = 'PostIt';*/
    
}
