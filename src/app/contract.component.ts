import { Component } from '@angular/core';
import { Router } from '@angular/router';

@Component({
  selector: 'contract',
  moduleId: module.id,
  styleUrls: ['./all.component.css'],
  templateUrl: './contract.component.html',

})

export class ContractComponent  { /*name = 'PostIt';*/
    
}
